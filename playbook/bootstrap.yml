---
# Bootstrap python-apt for apt module
- hosts: all
  become: yes
  tasks:
  - name:
    command: apt-get install -yq python3-apt
  - name: Setup local hosts file
    include_tasks: tasks/setup_hosts.yml

